# Learnly backend

The backend will use FastAPI and Python. It will handle PostgreSQL persistence, PDF processing, and document intelligence. Backend implementation is intentionally not included yet. The frontend will communicate with it through REST APIs.

