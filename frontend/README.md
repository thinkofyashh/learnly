# Learnly frontend

The Learnly web experience is built with Next.js, React, TypeScript, the App Router, and CSS Modules. It currently uses centralized mock data while retaining a native Fetch client for the planned REST API.

Run `npm install`, then `npm run dev`. Copy `.env.local.example` to `.env.local` when connecting a backend.

