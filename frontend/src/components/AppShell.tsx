"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { ThemeToggle } from "./ThemeToggle"; import styles from "./AppShell.module.css";
const links=[{href:"/",label:"Overview",icon:"⌂"},{href:"/notes",label:"Library",icon:"▤"},{href:"/admin",label:"Admin",icon:"◇"},{href:"/admin/upload",label:"Upload",icon:"↑"},{href:"/admin/documents",label:"Processing",icon:"◌"}];
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className={styles.shell}><aside className={styles.sidebar}><Link href="/" className={styles.brand}>Learnly<span>.</span></Link><p className={styles.eyebrow}>Learning archive</p><nav aria-label="Primary navigation">{links.map(l=><Link key={l.href} href={l.href} className={`${styles.link} ${path===l.href?styles.active:""}`}><span aria-hidden>{l.icon}</span>{l.label}</Link>)}</nav><div className={styles.sidebarFoot}><span className={styles.dot}/>Frontend preview<br/><small>Mock library data</small></div></aside><div className={styles.workspace}><header className={styles.header}><Link href="/" className={styles.mobileBrand}>Learnly.</Link><div><span className={styles.kicker}>Your knowledge, thoughtfully kept</span></div><div className={styles.headerActions}><ThemeToggle/><Link className={styles.upload} href="/admin/upload">Upload note</Link></div></header><main className={styles.main}>{children}</main><nav className={styles.mobileNav} aria-label="Mobile navigation">{links.slice(0,3).map(l=><Link key={l.href} href={l.href} className={path===l.href?styles.mobileActive:""}><span aria-hidden>{l.icon}</span>{l.label}</Link>)}</nav></div></div>}
